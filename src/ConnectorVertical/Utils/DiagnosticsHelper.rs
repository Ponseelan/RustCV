pub struct DiagnosticsHelper
{
    
}

impl DiagnosticsHelper
{
  pub fn Merge(diagnostics: Vec<Diagnostics>) -> Diagnostics
  {
   let diagnostics = Diagnostics::new();
   //initialize list of Event objects
    let events = Vec::new();
    
  }
}